# ── Server ───────────────────────────────────────────
PORT=3456
HOST=0.0.0.0

# ── Authentication ───────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_TOKEN=change-me-to-a-random-secret

# ── Claude CLI ───────────────────────────────────────
# Path to the claude binary (auto-detected if on PATH)
# CLAUDE_PATH=/usr/local/bin/claude

# Default project directory for tasks that don't specify one
# In native mode, defaults to current working directory if not set
# In Docker mode, defaults to /projects (the mounted volume)
# DEFAULT_PROJECT_DIR=/Users/you/projects

# Max turns per task (prevents runaway tasks)
MAX_TURNS=50

# Task timeout in milliseconds (default: 30 minutes)
TASK_TIMEOUT_MS=1800000

# ── Claude Auth ─────────────────────────────────────
# Native mode: Claude uses your system Keychain automatically (no config needed)
# Docker mode: Set your API key (Keychain is not accessible in containers)
# ANTHROPIC_API_KEY=sk-ant-...

# ── Database ─────────────────────────────────────────
# Native mode: defaults to ~/.claude-daemon/daemon.db (auto-created)
# Docker mode: defaults to /data/daemon.db
# DB_PATH=/custom/path/daemon.db

# ── Email Notifications (optional) ───────────────────
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx
NOTIFY_TO=your.email@gmail.com

# ── Docker-only options ──────────────────────────────
# Domain for HTTPS via Caddy (default: localhost with self-signed cert)
# DAEMON_DOMAIN=claude.yourdomain.com

# Host directory containing your projects (mounted into container as /projects)
# PROJECTS_DIR=/Users/you/Desktop

# ── Tailscale (optional — for remote access from anywhere) ─
# Generate at: https://login.tailscale.com/admin/settings/keys
# TS_AUTHKEY=tskey-auth-xxxxx
