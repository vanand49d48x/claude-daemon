# ── Server ───────────────────────────────────────────
PORT=3456
HOST=0.0.0.0

# ── Authentication ───────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_TOKEN=change-me-to-a-random-secret

# ── Claude CLI ───────────────────────────────────────
# Path to the claude binary (auto-detected if on PATH)
# CLAUDE_PATH=/usr/local/bin/claude

# Default project directory for tasks that don't specify one
DEFAULT_PROJECT_DIR=/projects

# Max turns per task (prevents runaway tasks)
MAX_TURNS=50

# Task timeout in milliseconds (default: 30 minutes)
TASK_TIMEOUT_MS=1800000

# ── Claude Auth (Option B — alternative to mounting ~/.claude) ─
# ANTHROPIC_API_KEY=sk-ant-...

# ── Email Notifications (optional) ───────────────────
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx
NOTIFY_TO=your.email@gmail.com

# ── Database ─────────────────────────────────────────
DB_PATH=/data/daemon.db

# ── Docker / Caddy ───────────────────────────────────
# Domain for HTTPS (default: localhost with self-signed cert)
# DAEMON_DOMAIN=claude.yourdomain.com

# Host directory containing your projects (mounted into container)
# PROJECTS_DIR=/Users/you/projects

# ── Tailscale (optional — for remote access from anywhere) ─
# Generate at: https://login.tailscale.com/admin/settings/keys
# TS_AUTHKEY=tskey-auth-xxxxx
